import { OnDestroyEmitter } from './on-destroy-emitter';

describe('OnDestroyEmitter', () => {
    it('should create an instance', () => {
        expect(new OnDestroyEmitter()).toBeTruthy();
    });
});
